package com.acme.app.common;

/**
 * Enum — exercises the EnumCollector. Should surface as an ENUM node with
 * the three constants PLACED, PAID, SHIPPED.
 */
public enum OrderStatus {
    PLACED,
    PAID,
    SHIPPED
}
